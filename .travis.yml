language: node_js
node_js:
  - "8.9.3"
  - "10"

branches:
  only:
    - master
    - /^greenkeeper/.*$/

cache: false
    
before_script:
  - npm install -g @angular/cli

script:
  - ng test --watch false --code-coverage
  - npn run e2e
  - ng build --prod
